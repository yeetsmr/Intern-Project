<Window x:Class="ClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Intern-Project WPF Task Management" Height="500" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Task Management System" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListView Grid.Row="1" Name="TaskListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="80"/>
                    <GridViewColumn Header="Task Name" DisplayMemberBinding="{Binding TaskName}" Width="200"/>
                    <GridViewColumn Header="Estimated Time(Hours)" DisplayMemberBinding="{Binding MaxTime}" Width="150"/>
                    <GridViewColumn Header="Priorty" DisplayMemberBinding="{Binding pri}" Width="100"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding IsCompleted}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>